import PageTemplate from 'components/Common/PageTemplate';
import ShortURL from 'components/ShortURL';

const ShortURLPage = (): JSX.Element => {
  return (
    <PageTemplate>
      <ShortURL />
    </PageTemplate>
  );
};

export default ShortURLPage;