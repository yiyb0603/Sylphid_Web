import PageTemplate from 'components/Common/PageTemplate';
import SelectType from 'components/Search/SelectType';

const SearchTypePage = (): JSX.Element => {
  return (
    <PageTemplate>
      <SelectType />
    </PageTemplate>
  );
};

export default SearchTypePage;