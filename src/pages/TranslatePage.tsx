import PageTemplate from 'components/Common/PageTemplate';
import Translate from 'components/Translate';

const TranslatePage = (): JSX.Element => {
  return (
    <PageTemplate>
      <Translate />
    </PageTemplate>
  );
};

export default TranslatePage;